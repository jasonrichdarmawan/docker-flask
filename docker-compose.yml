version: "3"

services:
  app:
    build: .
    depends_on:
      - nginx
  nginx:
    image: nginx:1.22
    ports:
      - 80:80
      - 443:443
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - /etc/letsencrypt/live/somas.godata.id:/etc/letsencrypt/live/somas.godata.id